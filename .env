# Local development — change to https://api.transkey.app for production
TRANSKEY_API_URL=https://api.transkey.app

# Google Sign-In: the WEB OAuth client ID (same value as the server's
# GOOGLE_CLIENT_ID). Used as `serverClientId` so the idToken returned by the
# native SDK has an audience the server can verify.
GOOGLE_SERVER_CLIENT_ID=793172234051-81gcv59id4f6euc4u1riqrm0r65s06st.apps.googleusercontent.com
