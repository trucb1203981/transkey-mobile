<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only network policy — base-config still denies cleartext for any
  domain not listed here, but the LAN entries below let `flutter run` /
  `adb install` debug builds talk to the laptop's API over plain HTTP.
  This file is referenced exclusively from `src/debug/AndroidManifest.xml`
  via a `tools:replace` override on `networkSecurityConfig`, so release
  builds never see it.

  Add new dev hosts (e.g. another LAN IP) here, NOT in
  `src/main/res/xml/network_security_config.xml` (production).
-->
<network-security-config>
  <base-config cleartextTrafficPermitted="false" />
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">192.168.2.11</domain>
    <domain includeSubdomains="false">10.0.2.2</domain>
    <domain includeSubdomains="false">localhost</domain>
  </domain-config>
</network-security-config>
